const sequelize = require("../../helper/sequelizeHelper");

const getMyInfo= async(req, res, next)=>
{
    try{
        const email= req.user.Email;
        const data= await sequelize.que
    }
    catch(err)
    {

    }
}